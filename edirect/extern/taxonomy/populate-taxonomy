# Public domain notice for all NCBI EDirect scripts is located at:
# https://www.ncbi.nlm.nih.gov/books/NBK179288/#chapter6.Public_Domain_Notice

# populate-taxonomy

if [ -d "${extrasBase}" ]
then
  cd "${extrasBase}"

  env GOMEMORYLIMIT="${threeFourthMemory}GiB" rchive -gzip -input "taxoninfo.xml" -archive -db "$dbase" \
    -index "$index" -pattern "$name" < /dev/null
fi
